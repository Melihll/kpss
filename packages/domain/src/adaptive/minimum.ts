import type { MinimumPlanCandidate } from "./types";
function rank(c:MinimumPlanCandidate){if(c.kind==="revision"&&c.revisionUrgency==="critical_overdue")return 0;if(c.status==="partially_completed"&&c.importance==="core")return 1;if(c.kind==="task"&&c.importance==="core"&&c.status!=="completed")return 2;if(c.topicState==="remediation"||c.masteryLevel==="critical"||c.masteryLevel==="weak")return 3;if(c.kind==="revision"&&(c.revisionUrgency==="due"||c.revisionUrgency==="overdue"))return 4;if(c.importance==="core")return 5;return 6;}
export function buildMinimumDayPlan(input:{availableMinutes:number;candidates:readonly MinimumPlanCandidate[]}){if(input.availableMinutes<0)throw new Error("INVALID_AVAILABLE_MINUTES");let used=0;const selected=[] as MinimumPlanCandidate[];for(const c of [...input.candidates].filter(c=>c.minutes>0&&c.status!=="completed").sort((a,b)=>rank(a)-rank(b)||a.minutes-b.minutes||a.id.localeCompare(b.id))){if(used+c.minutes<=input.availableMinutes){selected.push(c);used+=c.minutes;}}return{tasks:selected,totalMinutes:used,reason:selected.length?"MINIMUM_MEANINGFUL_WORKLOAD":"NO_MEANINGFUL_TASK_FITS"};}
